README:
To run the posts project:

1) Start up the dev server: in the terminal, navigate to the server directory (cd src/server)
2) Type node index.js
3) In a separate terminal, from the project root:  npm start